# _bmad/scripts/render_skill.py

- RenderError · class · L26-L27 — class RenderError(ValueError)
- _hash_bytes · function · L36-L37 — def _hash_bytes(content: bytes) -> str
- _canonical_json · function · L40-L43 — def _canonical_json(value: Any) -> bytes
- _lookup · function · L46-L52 — def _lookup(data: dict[str, Any], dotted_path: str, label: str) -> Any
- _require_string · function · L55-L60 — def _require_string(value: Any, label: str, *, allow_empty: bool = False) -> str
- _require_string_list · function · L63-L69 — def _require_string_list(value: Any, label: str) -> list[str]
- _require_review_layers · function · L72-L95 — def _require_review_layers(value: Any, label: str) -> list[dict[str, str]]
- _load_sources · function · L98-L115 — def _load_sources(skill_dir: Path) -> dict[str, str]
- _resolve_config_value · function · L118-L125 — def _resolve_config_value(value: Any, label: str, project_root: Path) -> str
- _find_config_values · function · L128-L137 — def _find_config_values(data: Any, key: str, prefix: str = "") -> list[tuple[str, Any]]
- _resolve_short_config · function · L140-L150 — def _resolve_short_config( central: dict[str, Any], key: str, project_root: Path ) -> tuple[str, str]
- _format_markdown_list · function · L153-L161 — def _format_markdown_list(items: list[str]) -> str
- _format_review_layers · function · L164-L175 — def _format_review_layers(layers: list[dict[str, str]]) -> str
- _resolve_customization_value · function · L178-L189 — def _resolve_customization_value(value: Any, default: Any, label: str) -> tuple[Any, str]
- _resolve_replacements · function · L192-L229 — def _resolve_replacements( sources: dict[str, str], central: dict[str, Any], customization: dict[str, Any], defaults: dict[str, Any] | None, project_root: Path, ) -> tuple[dict[str, str], dict[str, Any]]
- _render_sources · function · L232-L267 — def _render_sources( sources: dict[str, str], replacements: dict[str, str], destination: Path ) -> dict[str, str]
- replace · function · L251-L261 — def replace(match: re.Match[str]) -> str
- _verify_existing · function · L270-L292 — def _verify_existing(destination: Path, manifest: dict[str, Any]) -> None
- _publish · function · L295-L319 — def _publish(destination: Path, outputs: dict[str, bytes], manifest: dict[str, Any]) -> None
- render · function · L322-L380 — def render(project_root: Path, skill_dir: Path) -> Path
- main · function · L383-L397 — def main() -> int
